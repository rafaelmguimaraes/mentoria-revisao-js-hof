/*
 Desafio 03
  Dado uma lista de animais de estimação (file: data/Pets.json)
  Retorne a soma das idades de todos os pets que não são cachorros nem gatos

  Resultado esperado: 
  43
  
*/

module.exports = getSumOfAgesNotCatNotDog = (listOfPets) => {
  // Desenvolva a solução aqui
  
  // Lembre-se de retornar o resultado esperado: return result;
};